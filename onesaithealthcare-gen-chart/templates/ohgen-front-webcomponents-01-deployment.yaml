kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Values.gen.app.name_front_webcomponents }}-webcomponents-front
  labels:
    app: {{ .Values.gen.app.name_front_webcomponents }}-webcomponents-front
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  replicas: {{ .Values.gen.app.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.gen.app.name_front_webcomponents }}-webcomponents-front
  template:
    metadata:
      labels:
        app: {{ .Values.gen.app.name_front_webcomponents }}-webcomponents-front
      annotations:
        sidecar.istio.io/inject: 'false'
    spec:
      imagePullSecrets:
        - name: {{ .Values.docker.registry.host }}
      serviceAccountName: oh-module
      containers:
        - name: ohgen-webcomponents-front
          image: {{ .Values.docker.registry.host }}/{{ .Values.docker.registry.project }}/ohgen-webcomponents-front:{{ .Values.gen.version.front_webcomponents_image_tag }}
          ports:
            - containerPort: 8080
              protocol: TCP

          livenessProbe:
            httpGet:
              path: /form-webcomponents/remoteEntry.js
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /form-webcomponents/remoteEntry.js
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20

          resources:
            limits:
              cpu: 50m
              memory: 48Mi
            requests:
              cpu: 25m
              memory: 16Mi
          imagePullPolicy: Always
