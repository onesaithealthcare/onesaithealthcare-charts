kind: Service
apiVersion: v1
metadata:
  name: '{{ .Values.ohhda.app.name }}'
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: '{{ .Values.ohhda.app.name }}'