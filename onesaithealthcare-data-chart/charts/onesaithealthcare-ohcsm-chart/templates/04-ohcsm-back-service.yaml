kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.ohcsm.app.back_name }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: prom
      protocol: TCP
      port: 9093
      targetPort: 9093
  selector:
    app: {{ .Values.ohcsm.app.back_name }}
