kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.bpm.app.name }}-smart-front
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: {{ .Values.bpm.app.name }}-smart-front
