kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Values.ohbpm.app.name }}-prof-front
  labels:
    app: {{ .Values.ohbpm.app.name }}-prof-front
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  replicas: {{ .Values.ohbpm.app.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.ohbpm.app.name }}-prof-front
  template:
    metadata:
      labels:
        app: {{ .Values.ohbpm.app.name }}-prof-front
      annotations:
        sidecar.istio.io/inject: 'false'
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.docker.registry.host }}
      serviceAccountName: oh-module
      containers:
        - name: ohbpm-prof-front
          image: {{ .Values.global.docker.registry.host }}/{{ .Values.global.docker.registry.project }}/ohbpm-prof-front:{{ .Values.ohbpm.version.prof_image_tag }}
          ports:
            - containerPort: 8080
              protocol: TCP

          livenessProbe:
            httpGet:
              path: /ohbpm-prof/index.html
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /ohbpm-prof/index.html
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20

          volumeMounts:
            - name:  {{ .Values.ohbpm.app.name }}-prof-front-cfg
              mountPath: /usr/share/nginx/html/ohbpm-prof/assets/config
              readOnly: true
            - name:  {{ .Values.ohbpm.app.name }}-prof-front-i18n
              mountPath: /usr/share/nginx/html/ohbpm-prof/res/i18n
              readOnly: true
            - name: hnhome-ohsso-front
              readOnly: true
              mountPath: /usr/share/nginx/html/ohbpm-prof/keycloak.json
              subPath: keycloak.json
            - name: nginx-front-cfg
              readOnly: true
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: ohsso-lib
              readOnly: true
              mountPath: /usr/share/nginx/html/ohbpm-prof/ohsso-lib.js
              subPath: ohsso-lib.js
            
          resources:
            limits:
              cpu: 50m
              memory: 48Mi
            requests:
              cpu: 25m
              memory: 16Mi
          imagePullPolicy: Always

      volumes:
        - name: ohbpm-prof-front-cfg
          configMap:
            name:  {{ .Values.ohbpm.app.name }}-prof-front-cfg
        - name: ohbpm-prof-front-i18n
          configMap:
            name:  {{ .Values.ohbpm.app.name }}-prof-front-i18n
        - name: hnhome-ohsso-front
          configMap:
            name: hnhome-ohsso-front
            defaultMode: 420
        - name: nginx-front-cfg
          configMap:
            name: nginx-front-cfg
            defaultMode: 420
        - name: ohsso-lib
          configMap:
            name: ohsso-lib
            defaultMode: 420
