kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.ohbpm.app.name }}-prof-front
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: {{ .Values.ohbpm.app.name }}-prof-front
