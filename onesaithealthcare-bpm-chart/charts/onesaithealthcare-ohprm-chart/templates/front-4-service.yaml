kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.ohprm.app.front_app }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: {{ .Values.ohprm.app.front_app }}
