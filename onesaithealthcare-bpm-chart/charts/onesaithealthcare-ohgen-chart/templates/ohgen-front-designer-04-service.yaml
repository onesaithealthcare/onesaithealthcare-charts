kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.ohgen.app.name_front_designer }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: {{ .Values.ohgen.app.name_front_designer }}
