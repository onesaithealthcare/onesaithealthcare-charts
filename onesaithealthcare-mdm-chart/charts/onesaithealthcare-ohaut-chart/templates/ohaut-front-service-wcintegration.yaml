kind: Service
apiVersion: v1
metadata:
  name: ohcore-webcomponents-integration-front
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: ohcore-webcomponents-integration-front
