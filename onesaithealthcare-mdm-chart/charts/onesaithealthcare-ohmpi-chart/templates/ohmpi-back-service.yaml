apiVersion: v1
kind: Service
metadata:
  name: ohmpi-back
  labels:
    app: ohmpi-back
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: prom
      protocol: TCP
      port: 9093
      targetPort: 9093
  selector:
    app: ohmpi-back
